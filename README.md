# AstrBot_siwu-arknights-query

明日方舟（Arknights）游戏数据查询插件，适用于 [AstrBot](https://github.com/Soulter/AstrBot)。由 Amiya-Bot 的同名查询插件移植而来，复用其 HTML 模板与数据源，通过 Playwright 无头浏览器截图出图。

支持 **Agent 主动调用**：在对话中让 AI 助手（Agent）自主判断并调用工具，自动发送查询生成的图片。

## 功能

- **干员查询**：星级/职业/天赋/技能/属性/档案（详情页）、精英化与专精材料、技能详情、召唤物信息
- **材料查询**：材料简介、合成公式（合成树）、可获取关卡与掉落概率、一图流推荐价值
- **敌方单位查询**：属性（血量/攻击/防御/法抗/移动速度等）、能力词条、关联单位
- **关卡查询**：关卡地图/敌方单位/掉落详情（支持关卡代号、关卡名、活动名、难度词如「突袭」「磨难」），同名多关卡让用户选序号，活动关卡列表、活动列表、生息演算地图
- **术语查询**：游戏术语/地形说明（如「晕眩」「束缚」「草丛」「高台」）
- **公招查询**：根据标签/文字推荐公开招募组合图（支持「高资」「资深」等口语、OCR 识别公招界面截图；推荐语 + 组合图一条消息发送）
- **代号记录**：支持登记干员/敌方单位的社区外号（如「夏游洁」→「予愿安洁莉娜」），查询时自动用代号记录解析用户输入，Agent 可自助登记/更新/删除

数据自动从 gitee 拉取（`amiya-bot-assets`）并解析到内存，无需手动更新。

## 安装

1. 在 AstrBot 管理面板 → 插件管理 → 安装插件 → 上传 zip（本仓库 Release 中的 `siwu-arknights-query-<version>.zip`）
2. 插件依赖 `playwright`，需在 AstrBot 环境安装 Chromium 浏览器内核：

   ```bash
   # 在 AstrBot 的 Python 环境执行（Windows 示例）
   python -m playwright install chromium
   ```

   > 依赖已固定为 `playwright==1.53.0`，与该内核版本匹配，请勿随意升级。

3. 插件激活后会自动在后台从 gitee 拉取游戏数据（首次拉取需要几分钟），完成后即可查询。

## 使用

### Agent 主动调用（推荐）

安装后，在支持 Agent 的 AstrBot 对话中直接提问，AI 会自动调用工具并发送图片：

- 「查一下干员 银灰」
- 「棘刺的专精材料要什么」
- 「W 的技能详情」
- 「银灰有召唤物吗」
- 「提纯源岩哪里刷」
- 「爱国者这个敌人的数据」
- 「查一下夏游洁」（若「夏游洁」是已登记的代号，会命中「予愿安洁莉娜」）
- 「1-7 怎么打」「CE-6 掉落」「SV-3 突袭」（关卡查询，自动发送关卡图片）
- 「骑兵与猎人的关卡」（活动关卡列表）
- 「晕眩是什么意思」「草丛地形」（术语/地形说明）
- 「帮我查公招」「公招 高资 群攻」或直接发送公招界面截图（自动 OCR 识别标签并推荐组合）

查询流程中的代号处理：

1. 查询工具会**先查代号记录**，命中社区外号/历史代号时直接解析为规范干员名
2. 未命中且本地无此名时，工具会引导 Agent **自行调用 AstrBot 的联网搜索工具**（`web_search_tavily`）确认该代号对应的官方名称
3. Agent 确认后调用代号管理工具**登记**（`夏游洁 → 予愿安洁莉娜`），后续查询直接命中
4. 用户反馈不对时，Agent 会**更新/删除**代号记录

### 命令回退（需 @ 机器人或唤醒词）

- `查干员 xxx` — 干员详情
- `查材料 xxx` — 材料资料
- `查敌人 xxx` — 敌方单位资料
- `查关卡 xxx` — 关卡资料（关卡代号/名称/活动/生息演算地图）
- `查术语 xxx` — 术语/地形说明
- `查公招 [标签]` — 公开招募推荐（支持文字标签，发送公招界面截图时自动 OCR 识别）

## 配置

| 配置项 | 说明 | 默认值 |
| --- | --- | --- |
| `akq_enabled` | 启用明日方舟查询（关闭后工具与命令不再响应） | `true` |
| `akq_auto_update` | 启动时自动拉取/更新游戏数据（关闭则使用本地已有数据） | `true` |
| `akq_repo_url` | 游戏数据仓库地址（Amiya-Bot 官方数据源，一般无需修改） | `https://gitee.com/amiya-bot/amiya-bot-assets.git` |
| `akq_auto_fetch_material_value` | 自动拉取材料价值数据（一图流 yituliu.cn） | `true` |
| `akq_render_width` | 渲染图片宽度（px）。HTML 模板为 1280px 固定画布，无需修改 | `1280` |
| `akq_render_timeout` | 渲染超时时间（秒） | `30` |

## 数据来源

- 游戏数据：<https://gitee.com/amiya-bot/amiya-bot-assets>（Amiya-Bot 官方数据仓库，随游戏版本自动更新）
- 材料价值：一图流 <https://yituliu.cn>

## 移植注意

- **Chromium 内核**：本机若缺少匹配的 Playwright 浏览器内核会渲染失败，必须执行 `python -m playwright install chromium`
- **git 依赖**：首次安装（无本地数据时）需通过 `git clone` 拉取数据源，机器需可用 git；已有数据时失败会回退本地缓存
- 渲染出图需要本机能访问本地文件与正常启动 Chromium（无头模式）

## 版本记录

| 版本 | 内容 |
| --- | --- |
| 1.0.13 | 修复公招截图 OCR 在 Windows 11 上失效的问题（此前判断 `platform.release()=="10"` 才执行 OCR，Windows 11 返回 `"11"` 导致 OCR 被永久跳过，标签只能靠 LLM 转述、经常丢词条只算到 3 个甚至 1 个）：① 去掉系统版本判断，OCR 工具存在即启用；② 截图 OCR 结果与 LLM 转述文本合并取并集，两者互补，保证五个词条全部参与组合计算；③ 支持引用消息（Reply.chain）内嵌截图识别 |
| 1.0.12 | 公招查询优化：① 修复渲染图片不全——截图前等待页面内所有图片加载完成（此前固定延时可能截到立绘空白的半成品图），等待条件用 `complete`，路径缺失的图不会阻塞；② 推荐语精简为一两句（只讲最优组合与最高稀有度）；③ 全部组合照常完整展示，不截断 |
| 1.0.11 | 新增公招查询（移植自 Amiya-Bot 招募插件）：Agent 工具 `arknights_query_recruit` + 命令回退「查公招」。支持文字标签（含「高资/资深/近战/远程」等口语缩写）与公招界面截图 OCR 识别（Windows 自带 `Windows.Media.Ocr.Cli.exe`），推荐语 + 组合图在同一条消息链发送（实现"AI 说话 + 发图"）；标签解析不依赖 jieba（特殊标签优先消费 + 普通标签最长包含匹配）；意图关键词与强制指令同步更新，用户表达公招意图即由 Agent 主动调用 |
| 1.0.10 | 修复查询回复重复发送两遍的问题（「奎隆地图」案例）：日志显示 Agent 调用了 AstrBot 内置 `send_message_to_user` 工具发送了一次，又把同一内容作为 LLM 最终回复发了一遍（文本带 `&&confused&&` 情绪标签导致框架内置去重失效）。① 强制指令新增规则 5【禁止重复发送】：禁止调用 `send_message_to_user` 等通用发消息工具、工具已发送结果后最终回复不得复述；② `on_llm_request` 钩子在查询意图请求中直接移除 `send_message_to_user` 工具作硬性兜底；③ 规则 3 新增【禁止反问关卡代号】：即使认为名称可能对应多关卡也必须立即调用查询工具，由工具返回候选，禁止先反问"具体是哪个关卡" |
| 1.0.8 | 修复 Agent 搜到数据后跳过查询工具直接发文字的问题（12:53「兔兔查大哥」案例）。① 预置默认代号表：大哥→重岳、二哥→望、大爹→爱国者、银老板→银灰、小羊→艾雅法拉、蒂蒂→斯卡蒂、拉狗→拉普兰德、德狗→德克萨斯等社区公认高频外号（加载时自动合并，只补缺不覆盖已登记内容），Agent 第一步调用查询工具即可命中直接出图；② 强制指令强化为四条执行规则：第一步必须调用查询工具、名称原样传参、未命中按引导三步执行、最终必须通过工具发送图片（只回复文字视为任务失败） |
| 1.0.7 | 修复 Agent 擅自改名的行为：12:47 日志显示用户问「二哥」时 Agent 擅自把名称猜成「棘刺」直接查询出图（绕过了未命中引导）。本次在强制指令与工具 docstring 中明确「名称原样传参」规则：用户明确给出的名称必须原样传入查询工具，禁止自行联想/替换/猜名；未命中时先联网确认官方名称再登记再查询。同时保留「未给名称时按上下文推断」的灵活性 |
| 1.0.6 | 补齐 1.0.5 遗漏：干员查询未命中分支（此前未随 1.0.5 打包生效）改为 `yield` 字符串回传 Agent；新增 `on_llm_request` 强制钩子——检测到查询意图时向 LLM 注入「必须调用查询工具出图、未命中时按引导三步执行」的系统指令，并确保三个查询工具在工具列表中，避免 Agent 只回文字或跳过工具调用 |
| 1.0.5 | 修复代号未命中时引导消息 Agent 收不到的问题：未命中引导与代号登记/删除/查询结果改为 `yield` 字符串直接作为工具返回值回传 Agent（此前走 `event.make_result()` 只发用户消息，Agent 只能收到「没有返回值」固定文案），Agent 现在能读到「联网搜索→登记代号→查询出图」三步引导并自动执行 |
| 1.0.4 | 修复代号未命中引导消息的 async 调用 bug（漏 await 导致消息构造抛异常，Agent 无法收到引导只能自行猜测）；Agent 查询路径改为完全信任确定匹配（代号记录/精确/英文名/索引号），禁用包含与相似度模糊匹配，避免社区简称误配（如「圣初雪」被包含匹配误判成「初雪」而非异格「圣聆初雪」）；查询工具 docstring 保持宽松，由 Agent 自行判断名称，可在已登记代号或上下文基础上灵活调用（如「再查一下他的专精材料」→ 直接查「予愿安洁莉娜」） |
| 1.0.3 | 代号解析改为「交给 Agent 判断」：Agent 查询路径不再用相似度匹配干员名（避免社区外号误匹配，如「夏游洁」误配「烈夏」），本地未命中时由 Agent 自行调用联网搜索确认规范名并登记代号后再查询；命令回退路径保留相似度兜底 |
| 1.0.2 | 新增干员/敌方单位代号（别名）记录：Agent 可查询/登记/删除社区外号，查询工具自动先用代号记录解析输入，未命中时提示联网搜索确认 |
| 1.0.1 | 修复专精材料图片缺失精英 1/2 图标问题（补齐 level/rank/classify 资源目录）；修复渲染视口高度 720 消除底部空白；默认渲染宽度统一为 1280 |
| 1.0.0 | 初版，从 Amiya-Bot 查询插件移植到 AstrBot，支持 Agent 主动调用 |

## 鸣谢

- 模板与数据源源自 [Amiya-Bot](https://github.com/AmiyaBot/Amiya-Bot) 的 arknights 查询插件
- 数据与图片版权归游戏厂商（Hypergryph）所有，仅用于学习交流
